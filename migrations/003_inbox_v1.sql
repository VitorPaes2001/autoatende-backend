-- 003_inbox_v1.sql
-- Versioning marker for Inbox v1.
-- No DDL changes here because target DB already contains required tables.

DO $$
BEGIN
  RAISE NOTICE 'Inbox v1 migration file created (no-op).';
END $$;
