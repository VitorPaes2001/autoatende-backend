# __AUTOATENDE_STRIPE_PHASE2E_COMMERCIAL_SITE_CHECKOUT_PAGES_V1__
# AutoAtendeAI commercial checkout site.
# Generated by autoatende_stripe_phase2e_commercial_site_checkout_pages_v1_20260707_020237.

server {
    listen 80;
    server_name autoatendeai.com.br www.autoatendeai.com.br;

        include /etc/nginx/conf.d/autoatende-acme-location.inc; # __AUTOATENDE_TLS_SAN_WEBROOT_STAGING_PREFLIGHT_V20_C1_TLS_B1_R4__

        location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name autoatendeai.com.br www.autoatendeai.com.br;
    include /etc/nginx/conf.d/autoatende-acme-location.inc; # __AUTOATENDE_TLS_SAN_WEBROOT_STAGING_PREFLIGHT_V20_C1_TLS_B1_R4__

    ssl_certificate /etc/nginx/conf.d/tls.crt;
    ssl_certificate_key /etc/nginx/conf.d/tls.key;

    root /etc/nginx/conf.d/commercial-site-root;
    index index.html;

    add_header X-AutoAtendeAI-Phase "autoatende_stripe_phase2e_commercial_site_checkout_pages_v1_20260707_020237" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location = /api {
        return 301 /api/;
    }

    location ^~ /api/ {
        proxy_pass http://whatsapp-api:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    location = /contratacao {
        return 301 /contratacao/;
    }

    location = /contratacao/sucesso {
        return 301 /contratacao/sucesso/;
    }

    location = /contratacao/cancelada {
        return 301 /contratacao/cancelada/;
    }

    location ^~ /assets/ {
        try_files $uri =404;
        expires 10m;
    }

    location /contratacao/ {
        try_files $uri $uri/ /contratacao/index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
